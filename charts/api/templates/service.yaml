apiVersion: v1
kind: Service
metadata:
  name: api
  labels: { app: api }
spec:
  type: NodePort
  selector: { app: api }
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      nodePort: {{ .Values.service.nodePort }}
