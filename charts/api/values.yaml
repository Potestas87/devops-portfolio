replicaCount: 2

image:
  repository: ghcr.io/potestas87/devops-portfolio-api
  tag: latest # bumped automatically by CI
  pullPolicy: IfNotPresent

service:
  port: 8080
  nodePort: 30080

resources:
  requests: { cpu: 50m, memory: 64Mi }
  limits: { cpu: 200m, memory: 128Mi }
